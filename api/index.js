// Vercel serverless entry. Vercel's Node runtime takes the exported Express
// app and invokes it for any request that hits the catch-all route in vercel.json.
module.exports = require('../server.js');
